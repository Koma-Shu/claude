# 必須
numpy>=1.24          # 音声のラウドネス解析

pillow>=10.0         # ffmpeg が libass を持たない場合のテロップ描画

# 任意（無くても他の工程は動く）
gdown>=5.1           # Google Drive からのダウンロード（rclone を使うなら不要）
yt-dlp>=2024.1.1     # お手本動画の取得（reference コマンド）
imageio-ffmpeg>=0.5  # ffmpeg が PATH に無い場合のフォールバック
